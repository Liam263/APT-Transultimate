export enum Role {
  Admin = 'super-admin',
  Customer = 'admin',
  General = 'general',
  User = 'user',
  Parent = 'parent',
  Tutor = 'tutor',
}

export enum CustomerStatus {
  Active = 'active',
  Inactive = 'inactive',
}

export enum GeneralStatus {
  Active = 'active',
  Inactive = 'inactive',
}
